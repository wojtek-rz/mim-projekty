res := 100

applyFuncion := (fun | (Int) -> Int) -> Void:
    res <- fun(res)

printInt(res) // 100
applyFuncion((x | Int) -> Int ( 2 * x)) // Short form, only expression
printInt(res) // 200
applyFuncion((x | Int) -> Int: {return x + 10;}) // Full form.
printInt(res) // 210