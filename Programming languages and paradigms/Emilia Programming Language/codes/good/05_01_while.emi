start := 1000
n := start
steps := 0
while n != 1:
    if n % 2 = 0:
        n <- n / 2
    else:
        n <- 3 * n + 1
    steps <- steps + 1

printStr("Collatz conjecture from " ++ intToStr(start) ++ 
         " gets to 1 in " ++ intToStr(steps) ++ " steps.")
