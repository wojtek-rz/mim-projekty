fib := (n | Int) -> Int:
    if n = 0:
        return 0
    if n = 1:
        return 1
    return fib(n - 1) + fib(n - 2)

n := 25
result := fib(n)
printStr("fib(" ++ intToStr(n) ++ ") = " ++ intToStr(result))
