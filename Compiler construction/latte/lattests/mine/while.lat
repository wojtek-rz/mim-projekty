int main(){
    int b = 16;
    int c = 1;
    int d = 2;
    while (b > 0) {
        b = b - 1;
        c = 2 * c;
        if (c > 10000 || c < 100) {
            d++;
        }
        else {
            d--;
        }
    }
    printInt(c);
    printInt(d);

    return 0;
}
